## Communication

- Be blunt and direct — no sugarcoating

## Environment

- Editor: Vim 9.1 with CoC.nvim (LSP via coc-tsserver, coc-eslint, coc-prettier), fzf+ripgrep, NERDTree, vim-fugitive
- Terminal: Ghostty
- Tailor suggestions to these tools (vim commands, not VS Code)

## Workflow

- Fix bugs autonomously — don't ask for hand-holding, just read logs/errors and resolve
- Use subagents to keep main context clean
- When fetching external resources and getting 403/401, use the `proxy-fetch` skill from Sweden

## Core Principles

- Simplicity first: make every change as simple as possible, minimal code impact
- Find root causes. No temporary fixes.
- Only touch what's necessary.
